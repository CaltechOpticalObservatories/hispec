# ADC Rotator — two Newport SMC100PP prism rotators, daisy-chained (hsfei)
#
# systemctl enable --now hispec-daemon@hsfei_adc
#
# Deploy the config alongside this file:
#   config/hsfei/hsfei_adc.yaml -> /etc/hispec/hsfei_adc.yaml
HISPEC_DAEMON=hsfei/adc
HISPEC_CONFIG=/etc/hispec/hsfei_adc.yaml
